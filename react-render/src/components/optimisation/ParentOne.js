import React,{useState} from "react"
import {Child} from './child'

export const ParentOne = ()=>{
    const [count,setCount] = useS
 }
 condele.log('parent one')
 return (
    <button = onClick= )
 )